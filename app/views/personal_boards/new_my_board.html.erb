<!-- Validation failure messages -->

<% if @personal_board.errors.any? %>
  <% @personal_board.errors.full_messages.each do |message| %>
    <div class="alert alert-dismissable alert-danger">
      <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
      <%= message %>
    </div>
  <% end %>
<% end %>

<!-- Responsive panel example -->

<div class="row">
  <div class="col-sm-offset-1 col-sm-10 col-md-offset-2 col-md-8">
    <div class="panel panel-default">
      <div class="panel-heading">
        <h3 class="panel-title">Before you begin, you should set up your Personal Board</h3>
      </div>
      <div class="panel-body">
        <form action="/create_personal_board" method="post" class="form-horizontal">
          <!-- Hidden input for authenticity token to protect from forgery -->
          <input name="authenticity_token" type="hidden" value="<%= form_authenticity_token %>">

          <!-- Label and input for boardmember5 -->
          <div class="form-group">
            <label for="boardmember5_id" class="col-sm-2 control-label">
              Boardmember5
            </label>

            <div class="col-sm-10">
              <%= select_tag(:boardmember5_id, options_from_collection_for_select(User.all, :id, :username), :class => "form-control") %>
            </div>


          </div>

          <!-- Label and input for boardmember4 -->
          <div class="form-group">
            <label for="boardmember4_id" class="col-sm-2 control-label">
              Boardmember4
            </label>
           <div class="col-sm-10">
              <%= select_tag(:boardmember4_id, options_from_collection_for_select(User.all, :id, :username), :class => "form-control") %>
            </div>
          </div>

          <!-- Label and input for boardmember3 -->
          <div class="form-group">
            <label for="boardmember3_id" class="col-sm-2 control-label">
              Boardmember3
            </label>
          <div class="col-sm-10">
              <%= select_tag(:boardmember3_id, options_from_collection_for_select(User.all, :id, :username), :class => "form-control") %>
            </div>
          </div>

          <!-- Label and input for boardmember2 -->
          <div class="form-group">
            <label for="boardmember2_id" class="col-sm-2 control-label">
              Boardmember2
            </label>
           <div class="col-sm-10">
              <%= select_tag(:boardmember2_id, options_from_collection_for_select(User.all, :id, :username), :class => "form-control") %>
            </div>
          </div>

          <!-- Label and input for boardmember1 -->
          <div class="form-group">
            <label for="boardmember1_id" class="col-sm-2 control-label">
              Boardmember1
            </label>
           <div class="col-sm-10">
              <%= select_tag(:boardmember1_id, options_from_collection_for_select(User.all, :id, :username), :class => "form-control") %>
            </div>
          </div>



          <div class="form-group">
            <div class="col-sm-offset-2 col-sm-10">
              <button class="btn btn-success">
                Create Personal Board
              </button>
              or
              <a href="/personal_boards">Cancel</a>
            </div>
          </div>
        </form>
      </div>
    </div>
  </div>
</div>

<!-- End responsive panel example -->
